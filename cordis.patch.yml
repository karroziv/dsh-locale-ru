# The dsh-locale-ru bundle patch: inserts the Russian language pack row.
# Applied after @deepseek-ai/dsh-web-app's layer (profile bundle order), so
# the row rides the same browser roster the modules node half scans into
# window.__DSH_BOOT__.
- insert:
    - id: dsh-locale-ru
      name: 'dsh-locale-ru'
